# Test Environment Variables
# Use this file for configuring the application for a test environment.
# Do NOT use real production secrets here.

# MongoDB Settings
MONGO_URI=mongodb://mongodb:27017
MONGO_DB_NAME=app_db_test

# JWT Authentication Settings
# Use dummy keys for testing. You can generate a test key pair if needed.
JWT_ALGORITHM=RS256
JWT_EXPIRATION_HOURS=1
JWT_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\nREPLACE_WITH_A_DUMMY_TEST_PRIVATE_KEY\n-----END RSA PRIVATE KEY-----"
JWT_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----\nREPLACE_WITH_A_DUMMY_TEST_PUBLIC_KEY\n-----END PUBLIC KEY-----"

# AWS S3 Storage Settings
# Use dummy credentials for a local or test S3 service (like MinIO) or a dedicated test bucket.
AWS_ACCESS_KEY_ID="test_access_key"
AWS_SECRET_ACCESS_KEY="test_secret_key"
S3_BUCKET_NAME="test-bucket-name"
AWS_REGION="us-east-1"

# CORS Origins
# Define which origins are allowed to connect to the backend.
ALLOWED_ORIGINS="http://localhost:3000,http://localhost:3001"

# Default Admin User
# Credentials for a default admin user to be created on startup (for testing).
ADMIN_EMAIL="admin_test@example.com"
ADMIN_PASSWORD="Admin@testpassword123"